# AGENT.md moved

Instructions now live under `.codex/` and are aggregated into `AGENTS.md` via `scripts/codex_sync.sh`.

Run `bash scripts/codex_sync.sh` (or `cargo make codex-sync`) after editing shards.
