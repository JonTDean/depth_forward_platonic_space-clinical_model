# Environment templates

Copy one of the `.example` files to the matching `.env.<namespace>.<profile>` name (e.g., `.env.app.web.api.dev`) and fill in secrets before running the corresponding crate. See `docs/runbook/web-quickstart.md` for the full workflow.
